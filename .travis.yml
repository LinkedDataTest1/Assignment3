language: java
jdk:
  - oraclejdk8
  
before_install:
  - chmod +x ./build/compileTasks.sh
  - chmod +x .api

install: true
  
script:
  - TASKS=(Task6 Task7)
  - touch err
  - ./build/compileTasks.sh ${TASKS[@]} > err
  
after_success:
  - ./.api true
  
after_failure:
  - ./.api false
  
